class Course {
  final String title;
  final String description;
  final String code; // We can store the emoji or icon name
  Course({
    required this.title,
    required this.description,
    required this.code,
  });
}